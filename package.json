{
  "name": "powerglow_vue",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start":"concurrently \"npm run dev --prefix api\" \"npm run serve --prefix powerGlow\"",
    "install-all": "npm install && npm install --prefix api && npm install --prefix powerGlow",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.4"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
